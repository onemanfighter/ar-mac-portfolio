import { Menu } from '@chakra-ui/react';
import { TopBarButton } from '@components';

const User = () => {
  return (
    <Menu>
      <TopBarButton
        text="Amit Raikwar"
        onClick={() => {}}
        ariaLabel="search-top-bar-button"
      />
    </Menu>
  );
};

export default User;
